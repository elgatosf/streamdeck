{
    "$id": "Manifest",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {
        "Controller": {
            "description": "Defines the controller type the action is applicable to. A **Keypad** refers to a standard action on a Stream Deck device, e.g. 1 of the 15 buttons on the Stream Deck MK.2, or a\npedal on the Stream Deck Pedal, whereas an **Encoder** refers to a dial / touchscreen on the Stream Deck+.",
            "enum": [
                "Encoder",
                "Keypad"
            ],
            "type": "string"
        },
        "DeviceType": {
            "description": "Stream Deck devices.",
            "enum": [
                0,
                1,
                2,
                3,
                4,
                5,
                6,
                7
            ],
            "type": "number"
        }
    },
    "description": "Defines the plugin and available actions, and all information associated with them, including the plugin's entry point, all iconography, action default behavior, etc.",
    "properties": {
        "Actions": {
            "description": "Collection of actions provided by the plugin, and all of their information; this can include actions that are available to user's via the actions list, and actions that are\nhidden to the user but available to pre-defined profiles distributed with the plugin (`Manifest.Actions.VisibleInActionsList`).",
            "items": {
                "properties": {
                    "Controllers": {
                        "description": "Defines the controller type the action is applicable to. A **Keypad** refers to a standard action on a Stream Deck device, e.g. 1 of the 15 buttons on the Stream Deck\nMK.2, or a pedal on the Stream Deck Pedal, whereas an **Encoder** refers to a dial / touchscreen on the Stream Deck+.",
                        "items": [
                            {
                                "$ref": "Manifest#/definitions/Controller"
                            },
                            {
                                "enum": [
                                    "Encoder",
                                    "Keypad"
                                ],
                                "type": "string"
                            }
                        ],
                        "maxItems": 2,
                        "minItems": 1,
                        "type": "array"
                    },
                    "DisableAutomaticStates": {
                        "description": "Determines whether the state of the action should automatically toggle when two states are defined. Default is `false`.",
                        "type": "boolean"
                    },
                    "DisableCaching": {
                        "description": "Determines whether Stream Deck should cache images associated with the plugin, and its actions. Default is `false`.",
                        "type": "boolean"
                    },
                    "Encoder": {
                        "description": "Provides information about how the action functions as part of an `Encoder` (dial / touchscreen).",
                        "properties": {
                            "Icon": {
                                "description": "Path to the image, with the file extension omitted, that will be displayed in the Stream Deck application in the circular canvas that represents the dial of the action.\nTODO: Define style guide.",
                                "type": "string"
                            },
                            "StackColor": {
                                "description": "Background color to display in the Stream Deck application when the action is part of a dial stack, and is the current action. Represented as a hexadecimal value.",
                                "type": "string"
                            },
                            "TriggerDescription": {
                                "description": "Descriptions that define the interaction of the action when it is associated with a dial / touchscreen on the Stream Deck+. This information is shown to the user.",
                                "properties": {
                                    "LongTouch": {
                                        "description": "Touchscreen \"long-touch\" interaction behavior description.",
                                        "type": "string"
                                    },
                                    "Push": {
                                        "description": "Dial \"push\" (press) interaction behavior description.",
                                        "type": "string"
                                    },
                                    "Rotate": {
                                        "description": "Dial rotation interaction behavior description.",
                                        "type": "string"
                                    },
                                    "Touch": {
                                        "description": "Touchscreen \"touch\" interaction behavior description.",
                                        "type": "string"
                                    }
                                },
                                "type": "object"
                            },
                            "background": {
                                "description": "Path to the image that will be displayed on the touchscreen behind the action's layout. **NB** This can be overridden by the user in the Stream Deck application.\nTODO: Define style guide.",
                                "type": "string"
                            },
                            "layout": {
                                "anyOf": [
                                    {
                                        "items": {
                                            "type": "string"
                                        },
                                        "type": "array"
                                    },
                                    {
                                        "enum": [
                                            "$A0",
                                            "$A1",
                                            "$B1",
                                            "$B2",
                                            "$C1",
                                            "$X1"
                                        ],
                                        "type": "string"
                                    }
                                ],
                                "description": "Name of a predefined layout, or the path to a JSON file that details a custom layout and its components, to be rendered on the action's touchscreen canvas. The\nlayout's component's values can be updated dynamically via `streamDeck.setFeedback`, and the layout can be changed entirely via `streamDeck.setFeedbackLayout`.\n- $X1, layout with the title at the top and the icon beneath it in the center.\n- $A0, layout with the title at the top and a full-width image canvas beneath it in the center.\n- $A1, layout with the title at the top, the icon on the left, and text value on the right.\n- $B1, layout with the title at the top, the icon on the left, and a text value on the right with a progress bar beneath it.\n- $B2, layout with the title at the top, the icon on the left, and a text value on the right with a gradient progress bar beneath it.\n- $C1, layout with the title at the top, and two rows that display an icon on the left and progress bar on the right (i.e. a double progress bar layout)."
                            }
                        },
                        "type": "object"
                    },
                    "Icon": {
                        "description": "Path to the image, with the file extension omitted, that will be displayed next to the action in the Stream Deck application's action list. The image must adhere to the\nfollowing style guidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 20x20 px and 40x40 px (@2x).\n- Be monochromatic, with foreground color of #EFEFEF and a transparent background.",
                        "type": "string"
                    },
                    "Name": {
                        "description": "Name of the action; this is displayed to the user in the actions list, and is used throughout the Stream Deck application to visually identify the action.",
                        "type": "string"
                    },
                    "PropertyInspectorPath": {
                        "description": "Optional path to the HTML file that represents the property inspector for this action; this is displayed to the user in the Stream Deck application when they add the\naction, allowing them to configure the action's settings. The plugin can send messages to the property inspector using `streamDeck.sendToPropertyInspector(context payload)`,\nand receive messages from the property inspector using `streamDeck.on(\"sendToPlugin\", (data) => ...`. When undefined, the manifest's top-level `PropertyInspectorPath`\nis used, otherwise none. **NB** Path should be relative to the root of the plugin's folder, with no leading slash.",
                        "items": {
                            "type": "string"
                        },
                        "type": "array"
                    },
                    "States": {
                        "description": "States the action can be in. When two states are defined the action will act as a toggle, with users being able to select their preferred iconography for each state.\n**NB** Automatic toggling of the state on action activation can be disabled by setting `DisableAutomaticStates` to `true`; the state can still be set from the plugin\nvia `streamDeck.setState(context, state)`.",
                        "items": [
                            {
                                "description": "Defines the state of the action; this includes behavior, iconography, typography, etc.",
                                "properties": {
                                    "FontFamily": {
                                        "description": "Default font-family to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "FontSize": {
                                        "description": "Default font-size to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "number"
                                    },
                                    "FontStyle": {
                                        "description": "Default font-style to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "enum": [
                                            "",
                                            "Bold",
                                            "Bold Italic",
                                            "Italic",
                                            "Regular"
                                        ],
                                        "type": "string"
                                    },
                                    "FontUnderline": {
                                        "description": "Determines whether the title associated with this state is underlined by default. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "boolean"
                                    },
                                    "Image": {
                                        "description": "Path to the image, with the file extension omitted, that will be displayed on the Stream Deck when this action's state is active. The image must adhere to the following style\nguidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 72x72 px and 144x144 px (@2x).\n\n**NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "MultiActionImage": {
                                        "description": "Path to the image, with the file extension omitted, that will be displayed when the action is being viewed as part of a multi-action. The image must adhere to the following\nstyle guidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 72x72 px and 144x144 px (@2x).\n\n**NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "Name": {
                                        "description": "Name of the state; when multiple states are defined this value is shown to the user when the action is being added to a multi-action. The user is then able to specify which\nstate they would like to activate as part of the multi-action.",
                                        "type": "string"
                                    },
                                    "ShowTitle": {
                                        "description": "Determines whether the title should be shown. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "boolean"
                                    },
                                    "Title": {
                                        "description": "Default title to be shown when the action is added to the Stream Deck.",
                                        "type": "string"
                                    },
                                    "TitleAlignment": {
                                        "description": "Default title alignment to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "enum": [
                                            "bottom",
                                            "middle",
                                            "top"
                                        ],
                                        "type": "string"
                                    },
                                    "TitleColor": {
                                        "description": "Default title color to be used when rendering the title of this state, represented a hexadecimal value. **NB** This can be overridden by the user in the Stream Deck\napplication.",
                                        "type": "string"
                                    }
                                },
                                "type": "object"
                            },
                            {
                                "description": "Defines the state of the action; this includes behavior, iconography, typography, etc.",
                                "properties": {
                                    "FontFamily": {
                                        "description": "Default font-family to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "FontSize": {
                                        "description": "Default font-size to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "number"
                                    },
                                    "FontStyle": {
                                        "description": "Default font-style to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "enum": [
                                            "",
                                            "Bold",
                                            "Bold Italic",
                                            "Italic",
                                            "Regular"
                                        ],
                                        "type": "string"
                                    },
                                    "FontUnderline": {
                                        "description": "Determines whether the title associated with this state is underlined by default. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "boolean"
                                    },
                                    "Image": {
                                        "description": "Path to the image, with the file extension omitted, that will be displayed on the Stream Deck when this action's state is active. The image must adhere to the following style\nguidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 72x72 px and 144x144 px (@2x).\n\n**NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "MultiActionImage": {
                                        "description": "Path to the image, with the file extension omitted, that will be displayed when the action is being viewed as part of a multi-action. The image must adhere to the following\nstyle guidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 72x72 px and 144x144 px (@2x).\n\n**NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "string"
                                    },
                                    "Name": {
                                        "description": "Name of the state; when multiple states are defined this value is shown to the user when the action is being added to a multi-action. The user is then able to specify which\nstate they would like to activate as part of the multi-action.",
                                        "type": "string"
                                    },
                                    "ShowTitle": {
                                        "description": "Determines whether the title should be shown. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "type": "boolean"
                                    },
                                    "Title": {
                                        "description": "Default title to be shown when the action is added to the Stream Deck.",
                                        "type": "string"
                                    },
                                    "TitleAlignment": {
                                        "description": "Default title alignment to be used when rendering the title of this state. **NB** This can be overridden by the user in the Stream Deck application.",
                                        "enum": [
                                            "bottom",
                                            "middle",
                                            "top"
                                        ],
                                        "type": "string"
                                    },
                                    "TitleColor": {
                                        "description": "Default title color to be used when rendering the title of this state, represented a hexadecimal value. **NB** This can be overridden by the user in the Stream Deck\napplication.",
                                        "type": "string"
                                    }
                                },
                                "type": "object"
                            }
                        ],
                        "maxItems": 2,
                        "minItems": 1,
                        "type": "array"
                    },
                    "SupportedInMultiActions": {
                        "description": "Determines whether the action is available to user's when they are creating multi-actions. Default is `true`.",
                        "type": "boolean"
                    },
                    "Tooltip": {
                        "description": "Tooltip shown to the user when they hover over the action within the actions list in the Stream Deck application.",
                        "type": "string"
                    },
                    "UUID": {
                        "description": "Unique identifier that identifies the action, represented in reverse-DNS format. This value is supplied by Stream Deck when events are emitted that relate to the action\nenabling you to identify the source of the event. **NB** This must be unique, and should be prefixed with the plugin's UUID.",
                        "type": "string"
                    },
                    "UserTitleEnabled": {
                        "description": "Determines whether the title field is available to the user when viewing the action's property inspector. Setting this to `False` will disable the user from specifying a\ntitle, thus allowing the plugin to have exclusive access via `streamDeck.setTitle(context, title, state, target)`. Default is `true`, i.e. the title field is enabled.",
                        "type": "boolean"
                    },
                    "VisibleInActionsList": {
                        "description": "Determines whether the action is available to user's via the actions list in the Stream Deck application. Setting this to `false` allows for the action to be used as\npart of pre-defined profiles distributed with the plugins, whilst not being available to users. Default is `true`.",
                        "type": "boolean"
                    }
                },
                "type": "object"
            },
            "type": "array"
        },
        "ApplicationsToMonitor": {
            "description": "Applications to monitor on Mac and Windows; upon a monitored application being launched or terminated, Stream Deck will notify the plugin.",
            "items": {
                "properties": {
                    "mac": {
                        "description": "Collection of applications to monitor on macOS.",
                        "items": {
                            "type": "string"
                        },
                        "type": "array"
                    },
                    "windows": {
                        "description": "Collection of applications to monitor on Windows.",
                        "type": "string"
                    }
                },
                "type": "object"
            },
            "type": "array"
        },
        "Author": {
            "description": "Author's name that will be displayed on the plugin's product page on the Marketplace.",
            "type": "string"
        },
        "Category": {
            "description": "Defines the actions list group, providing a natural grouping of the plugin's actions with the Stream Deck application's action list. **NB** This should be distinctive and\nsynonymous with your plugin, and it is therefore recommended that this be the same value as the plugin's `Name` field. When undefined, the actions will be available under the\n\"Custom\" group.",
            "type": "string"
        },
        "CategoryIcon": {
            "description": "Path to the image, with the file extension omitted, that will be displayed next to the action list group within the Stream Deck application. The icon should accurately represent\nthe plugin, and enable users to quickly identify the plugin. The icon must adhere to the following style guidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 28x28 px and 56x56 px (@2x).\n- Be monochromatic, with foreground color of #DFDFDF and a transparent background.",
            "type": "string"
        },
        "CodePath": {
            "description": "Path to the plugin's main entry point; this is executed when the Stream Deck application starts the plugin.",
            "type": "string"
        },
        "CodePathMac": {
            "description": "Path to the plugin's entry point specific to macOS; this is executed when the Stream Deck application starts the plugin on macOS.",
            "type": "string"
        },
        "CodePathWin": {
            "description": "Path to the plugin's entry point specific to Windows; this is executed when the Stream Deck application starts the plugin on Windows.",
            "type": "string"
        },
        "DefaultWindowSize": {
            "description": "Size of a window (`[width, height]`) opened when calling `window.open()` from the property inspector. Default value is `[500, 650]`.",
            "items": [
                {
                    "type": "number"
                },
                {
                    "type": "number"
                }
            ],
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
        },
        "Description": {
            "description": "Description of the plugin, and the functionality it provides, that will be displayed on the plugin's product page on the Marketplace.",
            "type": "string"
        },
        "Icon": {
            "description": "Path to the image, with the file extension omitted, that will be displayed on the Marketplace. The icon should accurately represent the plugin, stand out, and enable users to\nquickly identify the plugin. The icon must adhere to the following style guidelines.\n- Be in .PNG or .SVG format.\n- Be provided in two sizes, 288x288 px and 512x512 px (@2x).",
            "type": "string"
        },
        "Name": {
            "description": "Name of the plugin.",
            "type": "string"
        },
        "OS": {
            "description": "Collection of operating systems, and their minimum require versions, that the plugin supports.",
            "items": [
                {
                    "description": "Operating system that the plugin supports, and the minimum required version needed to run the plugin.",
                    "properties": {
                        "MinimumVersion": {
                            "description": "Minimum version required of the operating system to run the plugin.",
                            "type": "string"
                        },
                        "Platform": {
                            "description": "Operating system supported by the plugin.",
                            "enum": [
                                "mac",
                                "windows"
                            ],
                            "type": "string"
                        }
                    },
                    "type": "object"
                },
                {
                    "description": "Operating system that the plugin supports, and the minimum required version needed to run the plugin.",
                    "properties": {
                        "MinimumVersion": {
                            "description": "Minimum version required of the operating system to run the plugin.",
                            "type": "string"
                        },
                        "Platform": {
                            "description": "Operating system supported by the plugin.",
                            "enum": [
                                "mac",
                                "windows"
                            ],
                            "type": "string"
                        }
                    },
                    "type": "object"
                }
            ],
            "maxItems": 2,
            "minItems": 1,
            "type": "array"
        },
        "Profiles": {
            "description": "Collection of pre-defined profiles that are distributed with this plugin. Upon installation of the plugin, the user will be prompted to install the profiles. Once installed,\nthe plugin can switch any of the pre-defined plugins via `streamDeck.switchToProfile(name, device)`.",
            "items": {
                "properties": {
                    "DeviceType": {
                        "$ref": "Manifest#/definitions/DeviceType",
                        "description": "Type of device the profile is applicable too, e.g. Stream Deck+, Stream Deck Pedal, etc."
                    },
                    "DontAutoSwitchWhenInstalled": {
                        "description": "Determines whether the Stream Deck application should automatically switch to the profile when it is first installed. Default value is `false`.",
                        "type": "boolean"
                    },
                    "Name": {
                        "description": "Path to the `.streamDeckProfile`, with the file extension omitted, that contains the profiles layout and action settings.",
                        "type": "string"
                    },
                    "Readonly": {
                        "description": "Determines whether the profile is read-only, or if the user is able to customize it within the Stream Deck application. Default value is `false`.",
                        "type": "boolean"
                    }
                },
                "type": "object"
            },
            "type": "array"
        },
        "PropertyInspectorPath": {
            "description": "Optional path to the HTML file that represents the property inspector for all actions; this is displayed to the user in the Stream Deck application when they add an action,\nallowing them to configure the action's settings. The plugin can send messages to the property inspector using `streamDeck.sendToPropertyInspector(context payload)`, and\nreceive messages from the property inspector using `streamDeck.on(\"sendToPlugin\", (data) => ...`. **NB** Path should be relative to the root of the plugin's folder, with no\nleading slash.",
            "items": {
                "type": "string"
            },
            "type": "array"
        },
        "SDKVersion": {
            "const": 2,
            "description": "Preferred SDK version; this should _currently_ always be `2`.",
            "type": "number"
        },
        "Software": {
            "description": "Determines the Stream Deck software requirements for this plugin.",
            "properties": {
                "MinimumVersion": {
                    "description": "Minimum version of the Stream Deck application required for this plugin to run.",
                    "items": {
                        "type": "string"
                    },
                    "type": "array"
                }
            },
            "type": "object"
        },
        "URL": {
            "description": "Link to the plugin's website.",
            "type": "string"
        },
        "UUID": {
            "description": "Unique identifier used to identify the plugin, represented in reverse-DNS format.",
            "items": {
                "type": "string"
            },
            "type": "array"
        },
        "Version": {
            "description": "Version of the plugin, represented as a {@link https://semver.org semantic version}. **NB** pre-release values are not currently supported",
            "type": "string"
        }
    },
    "type": "object"
}

